# ──────────────────────────────────────────────
# twilio-phone-gateway — environment variables
# Copy to .env and fill in real values.
# ──────────────────────────────────────────────

# Server
PORT=8787

# Comma-separated E.164 phone numbers allowed to call/SMS.
# Leave blank to allow all (not recommended in production).
ALLOW_FROM=+15550001111,+15550002222

# ── Twilio ──────────────────────────────────────
# Required for async SMS follow-ups (when the agent takes >15 s).
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token_here

# Public base URL of this server as Twilio sees it (used to validate webhook signatures).
# Must match the URL configured in the Twilio console (e.g. your cloudflared tunnel URL).
# When set together with TWILIO_AUTH_TOKEN, all webhook requests are signature-verified.
PUBLIC_BASE_URL=https://twilio.i2dev.com

# Optional: override the sender number for outbound async SMS.
# If not set, the gateway uses the `To` field from the inbound webhook.
# TWILIO_SMS_FROM=+15550003333

# ── OpenClaw agent ──────────────────────────────
OPENCLAW_PHONE_SESSION_ID=phone
OPENCLAW_AGENT_ID=phone

# Max simultaneous agent calls (default: 10)
# OPENCLAW_MAX_CONCURRENT=10

# ── Display names ───────────────────────────────
# Used in voice greeting, Discord log labels, and agent prompt framing.
# Leave blank to omit names (e.g. "Phone: hello" instead of "Phone (Alice): hello").
# CALLER_NAME=Alice
# AGENT_NAME=Bot
# GREETING_TEXT=You are connected. Say something after the beep.

# ── Discord logging ─────────────────────────────
# Optional. When set, the gateway logs inbound speech and agent replies
# to this Discord channel via `openclaw message send`.
# Leave blank (or unset) to disable Discord logging entirely.
DISCORD_LOG_CHANNEL_ID=

# ── Tuning ──────────────────────────────────────
# Max SMS reply length in characters (default: 280)
# SMS_MAX_CHARS=280

# Time (ms) to wait for a fast SMS reply before acking and going async (default: 15000)
# SMS_FAST_TIMEOUT_MS=15000

# Max requests per phone number per window before rate-limiting (default: 20; 0 = disabled)
# RATE_LIMIT_MAX=20

# Rate limit window in milliseconds (default: 60000 = 1 minute)
# RATE_LIMIT_WINDOW_MS=60000
